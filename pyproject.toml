[project]
name = "stratecorum-terminal"
version = "0.1.0"
description = "Stratecorum as a high-color terminal game for iTerm."
requires-python = ">=3.11"

[project.scripts]
stratecorum-terminal = "stratecorum_terminal.app:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
